const fs = require('fs'); const content = fs.readFileSync('docs/.vitepress/theme/components/Panel/PanelPageGenerator.vue', 'utf8'); const template = content.substring(content.indexOf('<template>'), content.indexOf('</template>')); const lines = template.split('\n'); let depth = 0; for(let i=0; i<lines.length; i++){ const l = lines[i]; const o = (l.match(/<div\b/g) || []).length; const c = (l.match(/<\/div>/g) || []).length; if(o !== c) { depth += o - c; console.log(i+1, o-c>0?'+':'-', Math.abs(o-c), '=> depth:', depth, l.trim().substring(0, 50)); } }
