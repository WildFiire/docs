const fs = require('fs'); const content = fs.readFileSync('docs/.vitepress/theme/components/Panel/PanelPageGenerator.vue', 'utf8'); const template = content.substring(content.indexOf('<template>'), content.indexOf('</template>')); const opens = (template.match(/<div\b/g) || []).length; const closes = (template.match(/<\/div>/g) || []).length; console.log('Opens:', opens, 'Closes:', closes);
